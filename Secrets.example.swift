// TEMPLATE — copy to Mun/Secrets/Secrets.swift and fill in real keys.
// This file lives at the repo root (outside Mun/) so it is NOT compiled and
// causes no duplicate-symbol clash with the real, gitignored Secrets.swift.
import Foundation

enum Secrets {
    static let finnhubKey = ""   // free key from https://finnhub.io ; "" = US/ETFs stay on seed
}
